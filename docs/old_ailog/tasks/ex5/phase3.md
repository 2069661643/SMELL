- 额外要求，例如注释格式，文档撰写跟随前面的phase
- 取消下发公共样本步骤
- 每一轮(round)末尾进行投票
    - 提供vote-threshold参数接口，默认0.3，控制必定选中（小于vote-threshold）和必定丢弃（大于1-vote-threshold）
    - 稳健性检查：vote-threshold < sprase <= 0.5
    - 公共锚点打分是对实际预测器的打分进行+=
    - +inf/0/-inf设计
    - 初始全0
    - 按块打分
    - 每一个client投票来自每个id下全部重采样
        - 平均的方法参考grad，只不过我们这次传输的是每层预测器的输出
        - 每轮重采样相加后归一化至逐层sum为1
        - 每层（合计16）分别进行
        - 投票用的是源于对每层预测器的输出，而非其再加上公共锚点打分
    - 最后将一个round下全部的client的“投票”得分直接相加（client维），得到的做threshold，再下发预测器的公共锚点打分（+inf/0/-inf）

